# utils — общие утилиты приложения
# Импортируй нужные модули напрямую:
#   from utils.db import load_data, load_basic_scores
#   from utils.names import variants, _norm
#   from utils.graph import build_index, rows_for, lineage
#   from utils.urls import build_share_url, share_button
#   from utils.ui import show_instruction, download_data_dialog, feedback_button
